<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<meta name="theme-color" content="#F8F7F4">
<title>김덕용 · 서비스 기획 포트폴리오</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;700;900&family=Noto+Sans+KR:wght@300;400;500&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
/* ══════════════════════════════════════
    TOKENS
══════════════════════════════════════ */
:root {
  --bg:        #FAFAFA;
  --surface:  #F2F4F8;
  --ink:       #0F1B35;
  --ink-2:    rgba(15,27,53,.45);
  --ink-3:    rgba(15,27,53,.22);
  --accent:    #2D5BE3;
  --accent-dim: rgba(45,91,227,.18);
  --line:      rgba(15,27,53,.1);
  --epo:       cubic-bezier(0.16,1,0.3,1);
  --spr:       cubic-bezier(0.34,1.56,0.64,1);
  /* alias — 이 변수들을 쓰던 자리를 accent로 연결 */
  --gold:      #2D5BE3;
  --gold-dim: rgba(45,91,227,.18);
}

/* ══════════════════════════════════════
    RESET
══════════════════════════════════════ */
*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }
html, body { background: var(--bg); }
body {
  color: var(--ink);
  font-family: 'Noto Sans KR', sans-serif;
  height: 100dvh; overflow: hidden;
  -webkit-font-smoothing: antialiased;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  #c-dot, #c-ring, #ripple { display: none !important; }
}

/* ══════════════════════════════════════
    CUSTOM CURSOR
══════════════════════════════════════ */
#c-dot, #c-ring {
  position: fixed; border-radius: 50%;
  pointer-events: none; z-index: 9999;
  top: 0; left: 0; opacity: 0; transition: opacity .3s;
}
#c-dot {
  width: 8px; height: 8px; background: var(--accent);
  margin-left: -4px; margin-top: -4px;
  box-shadow: 0 0 0 2px rgba(255,255,255,.9), 0 0 12px rgba(45,91,227,.5);
}
#c-ring {
  width: 36px; height: 36px;
  border: 1.5px solid rgba(45,91,227,.5);
  margin-left: -18px; margin-top: -18px;
  transition: opacity .3s, width .25s var(--epo), height .25s var(--epo), margin .25s var(--epo);
}
#c-ring.big { width:54px; height:54px; margin-left:-27px; margin-top:-27px; border-color:var(--accent); }

/* ══════════════════════════════════════
    RIPPLE
══════════════════════════════════════ */
#ripple { position:fixed; inset:0; pointer-events:none; z-index:1; }

/* ══════════════════════════════════════
    NAV
══════════════════════════════════════ */
nav {
  position: fixed; top: 0; width: 100%;
  padding: clamp(1.2rem,3dvh,1.8rem) clamp(1.5rem,4vw,3rem);
  z-index: 500;
  display: flex; align-items: center; justify-content: space-between;
  transition: background .35s, backdrop-filter .35s;
}
/* 어두운 배경 섹션(about, contact)에서 nav 색 전환 */
nav.on-dark .nav-logo { color: rgba(255,255,255,.55); }
nav.on-dark .nav-logo:hover { color: #fff; }
nav.on-dark .nav-logo span { color: var(--accent); }
nav.on-dark .ham span { background: rgba(255,255,255,.75); }
nav.on-dark a.nl { color: rgba(255,255,255,.45); }
nav.on-dark a.nl:hover { color: #fff; }
nav.on-dark a.nl.active { color: var(--accent); }
.nav-logo {
  font-family: 'DM Mono', monospace;
  font-size: .72rem; letter-spacing: .16em;
  color: var(--ink-2); text-decoration: none;
  transition: color .3s;
}
.nav-logo:hover { color: var(--ink); }
.nav-logo span { color: var(--accent); }
.ham {
  display: none; flex-direction: column; gap: 5px;
  background: none; border: none; padding: 8px;
  min-width: 44px; min-height: 44px;
  align-items: center; justify-content: center;
  z-index: 510; cursor: pointer;
}
.ham span {
  display: block; width: 22px; height: 1.5px;
  background: var(--ink);
  transition: transform .3s var(--epo), opacity .3s;
}
.ham.open span:nth-child(1) { transform: translateY(6.5px) rotate(45deg); }
.ham.open span:nth-child(2) { opacity: 0; }
.ham.open span:nth-child(3) { transform: translateY(-6.5px) rotate(-45deg); }
.nav-links { display: flex; align-items: center; gap: 2.5rem; }
nav a.nl {
  font-size: .68rem; font-weight: 500;
  letter-spacing: .18em; text-transform: uppercase;
  color: var(--ink-2); text-decoration: none;
  position: relative; padding: 6px 0;
  min-height: 44px; display: flex; align-items: center;
  transition: color .3s;
}
nav a.nl::after {
  content: ''; position: absolute;
  bottom: 0; left: 0; height: 1px; width: 0;
  background: var(--accent); transition: width .3s var(--epo);
}
nav a.nl:hover { color: var(--ink); }
nav a.nl:hover::after, nav a.nl.active::after { width: 100%; }
nav a.nl.active { color: var(--accent); }

@media (max-width: 640px) {
  .ham { display: flex; }
  .nav-links {
    position: fixed; inset: 0;
    background: rgba(248,247,244,0.97);
    backdrop-filter: blur(16px);
    flex-direction: column; justify-content: center; gap: 2.5rem;
    opacity: 0; pointer-events: none;
    transition: opacity .3s; z-index: 498;
  }
  .nav-links.open { opacity: 1; pointer-events: all; }
  nav a.nl { font-size: 1.1rem; }
}

/* ══════════════════════════════════════
    SCROLL WRAPPER
══════════════════════════════════════ */
.wrap {
  height: 100dvh;
  overflow-y: scroll; overflow-x: hidden;
  scroll-snap-type: y mandatory;
  scroll-snap-stop: always;
  -webkit-overflow-scrolling: touch;
  overscroll-behavior-y: contain;
  scrollbar-width: none;
}
.wrap::-webkit-scrollbar { display: none; }

/* ══════════════════════════════════════
    SECTION BASE
══════════════════════════════════════ */
section {
  height: 100dvh;
  scroll-snap-align: start;
  scroll-snap-stop: always;
  position: relative;
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}

/* ══════════════════════════════════════
    ① MAIN HERO
══════════════════════════════════════ */
#main { background: var(--bg); }

/* 배경: 얇은 격자 */
.grid-bg {
  position: absolute; inset: 0;
  background-image:
    linear-gradient(rgba(26,24,20,.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(26,24,20,.04) 1px, transparent 1px);
  background-size: 64px 64px;
}
/* 블루 글로우 */
.glow {
  position: absolute;
  width: min(700px,90vw); aspect-ratio: 1;
  background: radial-gradient(circle, rgba(45,91,227,.08) 0%, transparent 65%);
  top: 50%; left: 45%;
  transform: translate(-50%,-50%);
  pointer-events: none;
}

.main-inner {
  position: relative; z-index: 2;
  padding: 0 clamp(1.5rem,6vw,6rem);
  max-width: 1000px; width: 100%;
}

/* 상단 라벨 */
.eyebrow {
  display: flex; align-items: center; gap: .8rem;
  margin-bottom: 1.6rem;
  opacity: 0; animation: fadeUp .9s .1s var(--epo) forwards;
}
.eyebrow-tag {
  font-family: 'DM Mono', monospace;
  font-size: .65rem; letter-spacing: .18em; text-transform: uppercase;
  color: var(--accent);
  border: 1px solid rgba(45,91,227,.3);
  padding: .3rem .75rem;
}
.eyebrow-line {
  width: 40px; height: 1px;
  background: linear-gradient(to right, var(--accent), transparent);
}

/* 히어로 타이틀 */
h1.hero {
  font-family: 'Noto Serif KR', serif;
  font-size: clamp(2.8rem,7.5vw,6.8rem);
  font-weight: 900; line-height: 1.08;
  color: var(--ink); margin-bottom: 2rem;
  opacity: 0; animation: fadeUp .9s .25s var(--epo) forwards;
  letter-spacing: -.02em;
}
h1.hero .accent { color: var(--accent); font-style: italic; }
h1.hero .sub-line {
  display: block;
  font-size: clamp(1.2rem,2.8vw,2.6rem);
  font-weight: 400; color: var(--ink-2);
  letter-spacing: .01em; margin-top: .3rem;
}

/* 히어로 설명 */
.hero-desc {
  font-size: clamp(.85rem,1.4vw,.98rem);
  line-height: 1.95; color: var(--ink-2);
  max-width: 480px; font-weight: 300;
  opacity: 0; animation: fadeUp .9s .42s var(--epo) forwards;
}

/* CTA */
.cta-row {
  display: flex; align-items: center; gap: 1.5rem; flex-wrap: wrap;
  margin-top: 2.4rem;
  opacity: 0; animation: fadeUp .9s .58s var(--epo) forwards;
}
.btn-primary {
  display: inline-flex; align-items: center; gap: .6rem;
  padding: .85rem 2rem;
  background: var(--ink); color: var(--bg);
  font-size: .68rem; font-weight: 500;
  letter-spacing: .16em; text-transform: uppercase;
  text-decoration: none; border: none; cursor: pointer;
  transition: background .3s var(--epo), transform .3s var(--epo), box-shadow .3s;
}
.btn-primary:hover {
  background: var(--accent);
  transform: translateY(-2px);
  box-shadow: 0 14px 36px rgba(45,91,227,.28);
}
.btn-primary svg { width:13px; height:13px; stroke:currentColor; fill:none; stroke-width:2; transition:transform .3s var(--epo); }
.btn-primary:hover svg { transform: translateX(3px); }
.btn-ghost {
  font-size:.68rem; letter-spacing:.16em; text-transform:uppercase;
  color:var(--ink-2); text-decoration:none;
  display:inline-flex; align-items:center; gap:.4rem;
  transition:color .25s;
  padding: .85rem 0;
}
.btn-ghost:hover { color:var(--ink); }
.btn-ghost svg { width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:1.5;transition:transform .3s var(--epo); }
.btn-ghost:hover svg { transform:translateX(4px); }

/* 우측 숫자 배지 (데스크탑) */
.hero-kpi {
  position: absolute; right: clamp(1.5rem,5vw,5rem); bottom: 12dvh;
  display: flex; flex-direction: column; gap: 1.6rem;
  opacity: 0; animation: fadeUp .9s .75s var(--epo) forwards;
  z-index: 2;
}
@media (max-width: 768px) { .hero-kpi { display: none; } }
.kpi-item { text-align: right; }
.kpi-num {
  font-family: 'Noto Serif KR', serif;
  font-size: clamp(2rem,3.5vw,3rem);
  font-weight: 900; color: var(--ink); line-height: 1;
}
.kpi-num sup { font-size: .55em; color: var(--accent); vertical-align: super; }
.kpi-lbl {
  font-size: .6rem; letter-spacing: .2em; text-transform: uppercase;
  color: var(--ink-3); margin-top: .25rem;
}
.kpi-divider { width: 1px; height: 20px; background: var(--line); margin-left: auto; }

/* 스크롤 힌트 */
.scroll-hint {
  position: absolute; bottom: 2rem;
  left: clamp(1.5rem,6vw,6rem);
  display: flex; align-items: center; gap: 1rem;
  opacity: 0; animation: fadeUp .9s 1s var(--epo) forwards;
}
.scroll-track { width:1px; height:48px; background:rgba(45,91,227,.12); position:relative; overflow:hidden; }
.scroll-fill  { position:absolute;top:0;left:0;width:100%;height:100%;background:var(--accent);animation:drop 2s var(--epo) infinite; }
.scroll-lbl   { font-family:'DM Mono',monospace;font-size:.56rem;letter-spacing:.22em;text-transform:uppercase;color:rgba(45,91,227,.45);writing-mode:vertical-rl;transform:rotate(180deg); }

/* ══════════════════════════════════════
    ② ABOUT
══════════════════════════════════════ */
#about {
  background: var(--ink);
  flex-direction: column;
  padding: 0 clamp(1.5rem,6vw,6rem);
}
#about::before {
  content: '';
  position: absolute; right: 0; top: 0; bottom: 0;
  width: 40%;
  background: linear-gradient(to left, rgba(45,91,227,.05), transparent);
  pointer-events: none;
}

.about-wrap {
  max-width: 1100px; width: 100%;
  display: grid; grid-template-columns: 1.1fr .9fr;
  gap: clamp(3rem,6vw,7rem); align-items: center;
  position: relative; z-index: 2;
}
@media (max-width: 780px) {
  .about-wrap { grid-template-columns:1fr; gap:2.5rem; }
}

/* 왼쪽 */
.about-eyebrow {
  font-family: 'DM Mono', monospace;
  font-size: .62rem; letter-spacing: .25em; text-transform: uppercase;
  color: var(--accent); margin-bottom: 1.4rem;
  display: flex; align-items: center; gap: .7rem;
}
.about-eyebrow::before {
  content:''; width:28px; height:1px; background:var(--accent); display:inline-block;
}
.about-title {
  font-family: 'Noto Serif KR', serif;
  font-size: clamp(1.9rem,3.8vw,3.2rem);
  font-weight: 900; color: #fff; line-height: 1.2;
  margin-bottom: 1.6rem; letter-spacing: -.015em;
}
.about-title em { color: var(--accent); font-style: normal; }
.about-bio {
  font-size: clamp(.82rem,1.3vw,.93rem);
  line-height: 2; color: rgba(255,255,255,.45);
  font-weight: 300; margin-bottom: 2.2rem;
}
.about-bio strong { color: rgba(255,255,255,.8); font-weight: 500; }

/* 태그 목록 */
.about-tags {
  display: flex; flex-wrap: wrap; gap: .5rem;
}
.about-tag {
  font-family: 'DM Mono', monospace;
  font-size: .6rem; letter-spacing: .14em;
  color: rgba(45,91,227,.85);
  border: 1px solid rgba(45,91,227,.25);
  padding: .28rem .7rem;
}

/* 오른쪽: 통계 + 스킬 */
.about-right {}
.stats-row {
  display: grid; grid-template-columns: repeat(3,1fr);
  border: 1px solid rgba(255,255,255,.08);
  margin-bottom: 1.5rem;
}
.stat-box {
  padding: 1.4rem 1rem;
  border-right: 1px solid rgba(255,255,255,.08);
  text-align: center;
}
.stat-box:last-child { border-right: none; }
.stat-n {
  font-family: 'Noto Serif KR', serif;
  font-size: clamp(1.6rem,3vw,2.4rem);
  font-weight: 900; color: #fff; line-height: 1;
}
.stat-n sup { font-size:.55em; color:var(--accent); vertical-align:super; }
.stat-l {
  font-size:.58rem;letter-spacing:.18em;text-transform:uppercase;
  color:rgba(255,255,255,.3);margin-top:.3rem;
}

.skill-list { display: flex; flex-direction: column; gap: .55rem; }
.skill-row {
  display: flex; align-items: center; gap: 1rem;
  padding: .9rem 1.1rem;
  background: rgba(255,255,255,.03);
  border: 1px solid rgba(255,255,255,.06);
  transition: background .3s, border-color .3s;
  cursor: default;
}
.skill-row:hover {
  background: rgba(45,91,227,.08);
  border-color: rgba(45,91,227,.25);
}
.skill-dot {
  width: 6px; height: 6px; border-radius: 50%;
  background: var(--accent); flex-shrink: 0;
}
.skill-txt { font-size:.78rem; color:rgba(255,255,255,.65); line-height:1.4; }
.skill-txt strong { color:rgba(255,255,255,.9); font-weight:500; display:block; }

/* ══════════════════════════════════════
    ③ 포트폴리오 섹션 공통
══════════════════════════════════════ */
.port {
  flex-direction: column;
  padding: clamp(3.5rem,7dvh,5.5rem) clamp(1.5rem,5vw,4rem) clamp(2rem,4dvh,3rem);
  gap: clamp(1rem,2dvh,2rem);
  justify-content: center;
}
.port:nth-child(odd)  { background: var(--bg); }
.port:nth-child(even) { background: var(--surface); }
.port::before {
  content: ''; position: absolute; left: 0; top: 15%; bottom: 15%;
  width: 2px;
  background: linear-gradient(to bottom, transparent, rgba(45,91,227,.25), transparent);
}

.sec-head { width:100%;max-width:1200px;display:flex;align-items:baseline;gap:1.2rem; }
.sec-num {
  font-family:'DM Mono',monospace;
  font-size:clamp(1.8rem,3.5vw,3rem);
  font-weight:400;color:rgba(15,27,53,.08);
  line-height:1;flex-shrink:0;user-select:none;letter-spacing:-.02em;
}
.sec-meta { display:flex; flex-direction:column; gap:.3rem; }
.sec-lbl  { font-family:'DM Mono',monospace;font-size:.6rem;letter-spacing:.24em;text-transform:uppercase;color:var(--accent); }
.sec-title{ font-family:'Noto Serif KR',serif;font-size:clamp(1.3rem,2.8vw,2.2rem);font-weight:700;color:var(--ink);line-height:1.2;letter-spacing:-.01em; }
.sec-desc { font-size:.78rem;color:var(--ink-2);line-height:1.7;max-width:520px;margin-top:.2rem;font-weight:300; }

/* ══════════════════════════════════════
    갤러리 그리드
══════════════════════════════════════ */
.gallery {
  width:100%;max-width:1200px;
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:clamp(.35rem,.7vw,.7rem);
}
@media(max-width:900px){ .gallery{grid-template-columns:repeat(3,1fr);} }
@media(max-width:560px){ .gallery{grid-template-columns:repeat(2,1fr);} }

.g-item {
  aspect-ratio:4/3;overflow:hidden;position:relative;
  outline:1px solid var(--line);outline-offset:-1px;
  transition:transform .5s var(--epo),box-shadow .5s var(--epo);
  cursor:pointer;
}
.g-item img {
  width:100%;height:100%;object-fit:cover;display:block;
  transition:transform .7s var(--epo),filter .5s;
  filter:brightness(.96) saturate(.85);
}
.g-over {
  position:absolute;inset:0;
  background:linear-gradient(160deg,rgba(45,91,227,.07) 0%,rgba(15,27,53,.55) 100%);
  display:flex;align-items:flex-end;padding:.9rem 1.1rem;
  opacity:0;transition:opacity .3s;
}
.g-over span {
  font-family:'DM Mono',monospace;
  font-size:.56rem;letter-spacing:.2em;text-transform:uppercase;
  color:rgba(250,250,250,.9);display:flex;align-items:center;gap:.4rem;
}
.g-over span::before { content:'';width:12px;height:1px;background:var(--accent);display:inline-block; }

@media(hover:hover){
  .g-item:hover img { transform:scale(1.05);filter:brightness(1) saturate(1); }
  .g-item:hover .g-over { opacity:1; }
  .g-item:hover { box-shadow:0 12px 40px rgba(15,27,53,.14),0 0 0 1px rgba(45,91,227,.2); }
}
@media(hover:none){ .g-item:active{transform:scale(.97);} }

/* ══════════════════════════════════════
    ④ CONTACT
══════════════════════════════════════ */
#contact { background: var(--ink); flex-direction:column; }
#contact::before {
  content:''; position:absolute; inset:0;
  background:
    radial-gradient(ellipse at 20% 80%, rgba(45,91,227,.07) 0%, transparent 55%),
    radial-gradient(ellipse at 80% 20%, rgba(45,91,227,.04) 0%, transparent 55%);
  pointer-events:none;
}
.con-inner { text-align:center;position:relative;z-index:2;padding:0 clamp(1.5rem,5vw,4rem);max-width:760px; }
.con-badge {
  display:inline-flex;align-items:center;gap:.5rem;
  font-family:'DM Mono',monospace;
  font-size:.6rem;letter-spacing:.22em;text-transform:uppercase;
  color:var(--accent);border:1px solid rgba(45,91,227,.3);padding:.4rem 1.1rem;margin-bottom:2rem;
}
.con-badge::before { content:'';width:6px;height:6px;background:var(--accent);border-radius:50%;animation:pulse 2s infinite; }
.con-title {
  font-family:'Noto Serif KR',serif;
  font-size:clamp(2rem,5.5vw,4.5rem);font-weight:900;color:#fff;line-height:1.12;margin-bottom:1.2rem;
  letter-spacing:-.02em;
}
.con-title em { color:var(--accent);font-style:italic; }
.con-desc { font-size:clamp(.8rem,1.3vw,.92rem);color:rgba(255,255,255,.4);line-height:1.9;font-weight:300;max-width:380px;margin:0 auto 2.5rem; }

.con-links { display:flex;gap:1rem;justify-content:center;flex-wrap:wrap; }
.con-link { display:flex;flex-direction:column;align-items:center;gap:.55rem;text-decoration:none;min-width:52px; }
.con-icon {
  width:52px;height:52px;
  border:1px solid rgba(255,255,255,.12);
  display:flex;align-items:center;justify-content:center;
  transition:background .3s,border-color .3s,transform .35s var(--spr);
}
.con-link:hover .con-icon { background:var(--accent);border-color:var(--accent);transform:translateY(-4px) scale(1.05); }
.con-icon svg { width:20px;height:20px;stroke:rgba(255,255,255,.6);fill:none;stroke-width:1.5;transition:stroke .3s; }
.con-link:hover .con-icon svg { stroke:#fff; }
.con-lbl { font-family:'DM Mono',monospace;font-size:.56rem;letter-spacing:.18em;text-transform:uppercase;color:rgba(255,255,255,.25); }

.con-foot { margin-top:2.5rem;display:flex;flex-direction:column;align-items:center;gap:.7rem; }
.con-line { width:60px;height:1px;background:linear-gradient(to right,transparent,rgba(45,91,227,.6),transparent); }
.con-copy { font-family:'DM Mono',monospace;font-size:.58rem;color:rgba(255,255,255,.15);letter-spacing:.1em; }

/* ══════════════════════════════════════
    팝업 (상세정보 + 포커스 트랩)
══════════════════════════════════════ */
.pop-ov {
  position:fixed;inset:0;
  background:rgba(10,9,7,.9);
  z-index:800;
  display:flex;align-items:center;justify-content:center;
  padding:clamp(1rem,4vw,3rem);
  opacity:0;pointer-events:none;transition:opacity .35s;
  backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px);
}
.pop-ov.open { opacity:1;pointer-events:all; }
.pop-box {
  position:relative;width:100%;max-width:960px;max-height:calc(100dvh - 4rem);
  background:#fff;
  overflow:hidden;display:flex;flex-direction:column;
  transform:translateY(22px) scale(.97);transition:transform .45s var(--spr);
}
.pop-ov.open .pop-box { transform:none; }
.pop-img-wrap { flex:1;overflow:hidden;min-height:0; }
.pop-img-wrap img { width:100%;height:100%;object-fit:cover;display:block;max-height:52dvh;transition:opacity .35s; }

.pop-info {
  padding:clamp(.9rem,2vw,1.5rem) clamp(1.2rem,3vw,2rem);
  flex-shrink:0;
  display:grid;
  grid-template-columns:1fr auto;
  align-items:start;
  gap:1rem;
  border-top:2px solid var(--accent);
}
.pop-title {
  font-family:'Noto Serif KR',serif;
  font-size:clamp(1rem,2.5vw,1.4rem);color:var(--ink);font-weight:700;
  letter-spacing:-.01em;
}
.pop-sub { font-family:'DM Mono',monospace;font-size:.65rem;color:var(--accent);margin-top:.2rem;letter-spacing:.1em; }
.pop-desc { font-size:.78rem;color:var(--ink-2);line-height:1.75;margin-top:.65rem;max-width:520px;font-weight:300; }
.pop-tags { display:flex;flex-wrap:wrap;gap:.35rem;margin-top:.7rem; }
.pop-tag {
  font-family:'DM Mono',monospace;
  font-size:.56rem;letter-spacing:.12em;
  color:var(--accent);border:1px solid rgba(45,91,227,.2);padding:.22rem .6rem;
}
.pop-info-right { display:flex;flex-direction:column;gap:.5rem;align-items:flex-end;flex-shrink:0; }
.pop-link {
  display:inline-flex;align-items:center;gap:.4rem;
  font-family:'DM Mono',monospace;
  font-size:.6rem;letter-spacing:.14em;text-transform:uppercase;
  color:var(--ink);text-decoration:none;
  border:1px solid var(--line);padding:.4rem .9rem;
  transition:background .25s,color .25s,border-color .25s;
}
.pop-link:hover { background:var(--accent);color:#fff;border-color:var(--accent); }
.pop-link svg { width:10px;height:10px;stroke:currentColor;fill:none;stroke-width:2; }
.pop-counter { font-family:'DM Mono',monospace;font-size:.6rem;color:var(--ink-3);letter-spacing:.1em;text-align:right; }

.pop-close {
  position:absolute;top:.85rem;right:.85rem;
  width:40px;height:40px;min-width:44px;min-height:44px;
  background:rgba(15,27,53,.06);border:1px solid var(--line);
  color:var(--ink);font-size:1rem;
  display:flex;align-items:center;justify-content:center;
  cursor:pointer;z-index:10;
  transition:background .2s,transform .3s var(--spr);
}
.pop-close:hover { background:var(--accent);color:#fff;transform:rotate(90deg); }
.pop-arr {
  position:absolute;top:38%;transform:translateY(-50%);
  width:44px;height:80px;background:rgba(255,255,255,.25);border:none;
  color:var(--ink);display:flex;align-items:center;justify-content:center;
  cursor:pointer;z-index:10;transition:background .2s;
}
.pop-arr:hover { background:rgba(45,91,227,.12); }
.pop-arr.prev { left:0;border-radius:0 4px 4px 0; }
.pop-arr.next { right:0;border-radius:4px 0 0 4px; }
.pop-arr svg { width:20px;height:20px;stroke:currentColor;fill:none;stroke-width:1.5; }

/* ══════════════════════════════════════
    TOP BTN & DOTS
══════════════════════════════════════ */
.top-btn {
  position:fixed;
  bottom:max(2rem,env(safe-area-inset-bottom,2rem));
  right:max(2rem,env(safe-area-inset-right,2rem));
  width:46px;height:46px;
  background:rgba(45,91,227,.08);border:1px solid rgba(45,91,227,.3);
  display:flex;align-items:center;justify-content:center;
  cursor:pointer;z-index:400;
  opacity:0;pointer-events:none;transform:translateY(14px);
  transition:opacity .35s var(--epo),transform .35s var(--epo),background .25s;
}
.top-btn.vis { opacity:1;pointer-events:all;transform:none; }
.top-btn:hover { background:var(--accent); }
.top-btn:hover svg { stroke:#fff; }
.top-btn svg { width:17px;height:17px;stroke:var(--accent);fill:none;stroke-width:2;transition:stroke .25s; }

.dots {
  position:fixed;
  left:max(1.5rem,env(safe-area-inset-left,1.5rem));
  top:50%;transform:translateY(-50%);
  display:flex;flex-direction:column;gap:.55rem;z-index:400;
}
.dot {
  width:4px;height:4px;border-radius:50%;
  background:rgba(15,27,53,.18);cursor:pointer;
  transition:background .3s,height .35s var(--spr),border-radius .35s;
  position:relative;
}
.dot[data-label]::after {
  content:attr(data-label);
  position:absolute; left:calc(100% + 10px); top:50%; transform:translateY(-50%);
  font-family:'DM Mono',monospace;
  font-size:.56rem;letter-spacing:.12em;text-transform:uppercase;
  color:var(--ink); background:var(--bg);
  border:1px solid var(--line); padding:.22rem .55rem;
  white-space:nowrap; opacity:0; pointer-events:none; transition:opacity .2s;
}
.dot:hover[data-label]::after { opacity:1; }
.dot.on { background:var(--accent);height:16px;border-radius:2px; }
@media(max-width:380px){ .dots{display:none;} }

/* ══════════════════════════════════════
    KEYFRAMES
══════════════════════════════════════ */
@keyframes fadeUp { from{opacity:0;transform:translateY(24px)} to{opacity:1;transform:none} }
@keyframes drop { 0%{transform:translateY(-100%)} 50%{transform:translateY(0)} 100%{transform:translateY(100%)} }
@keyframes pulse{ 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:.3;transform:scale(.5)} }
</style>
</head>
<body>

<div id="c-dot"></div>
<div id="c-ring"></div>
<canvas id="ripple"></canvas>

<nav>
  <a href="#main" class="nav-logo">KDY<span>.</span>portfolio</a>
  <button class="ham" id="ham" aria-label="메뉴" aria-expanded="false">
    <span></span><span></span><span></span>
  </button>
  <div class="nav-links" id="navLinks">
    <a href="#main"    class="nl active" data-nav="main">홈</a>
    <a href="#about"   class="nl"        data-nav="about">소개</a>
    <a href="#port-1"  class="nl"        data-nav="port">프로젝트</a>
    <a href="#contact" class="nl"        data-nav="contact">컨택트</a>
  </div>
</nav>

<div class="dots" id="dots"></div>

<div class="wrap" id="wrap">

  <section id="main" data-nav="main">
    <div class="grid-bg" id="gridBg"></div>
    <div class="glow"    id="glowEl"></div>
    <div class="main-inner">
      <div class="eyebrow">
        <span class="eyebrow-tag">Service Planning · UX Strategy</span>
        <span class="eyebrow-line"></span>
      </div>
      <h1 class="hero">
        Making<br>
        <span class="accent">ideas</span><br>visible.
      </h1>
      <p class="hero-desc">
        I think about the foundation that keeps a service running reliably —<br>
        before thinking about how it looks.<br>
        From NH Bank's next-gen build to leading full-cycle SM operations as PL.
      </p>
      <div class="cta-row">
        <a href="#port-1" class="btn-primary">
          View Projects
          <svg viewBox="0 0 24 24"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
        <a href="#about" class="btn-ghost">
          About Me
          <svg viewBox="0 0 24 24"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
      </div>
    </div>

    <div class="hero-kpi">
      <div class="kpi-item">
        <div class="kpi-num">8<sup>년</sup></div>
        <div class="kpi-lbl">총 경력</div>
      </div>
      <div class="kpi-divider"></div>
      <div class="kpi-item">
        <div class="kpi-num">NH<sup>+</sup></div>
        <div class="kpi-lbl">농협 차세대 PL</div>
      </div>
      <div class="kpi-divider"></div>
      <div class="kpi-item">
        <div class="kpi-num">5<sup>개사</sup></div>
        <div class="kpi-lbl">근무 이력</div>
      </div>
    </div>

    <div class="scroll-hint">
      <div class="scroll-track"><div class="scroll-fill"></div></div>
      <span class="scroll-lbl">Scroll</span>
    </div>
  </section>

  <section id="about" data-nav="about">
    <div class="about-wrap">
      <div class="about-left">
        <p class="about-eyebrow">Who I Am</p>
        <h2 class="about-title">
          Planner who puts<br>
          <em>reliability first.</em>
        </h2>
        <p class="about-bio">
          I believe a planner's job is to <strong>spot friction before users do, and fix it before it ships.</strong><br><br>
          8 years in financial SI taught me that a single mishandled edge case can erode trust in seconds.
          Today, I lead end-to-end planning for NH Corporate Banking's SM operations as <strong>PL (Project Leader)</strong> —
          from requirement alignment all the way through to release management.
        </p>
        <div class="about-tags">
          <span class="about-tag">금융 SI 기획</span>
          <span class="about-tag">UIUX 기획</span>
          <span class="about-tag">PL · PA 경험</span>
          <span class="about-tag">비대면 서비스</span>
          <span class="about-tag">Figma</span>
          <span class="about-tag">Notion</span>
          <span class="about-tag">사용자 인터뷰</span>
          <span class="about-tag">차세대 구축</span>
        </div>
      </div>
      <div class="about-right">
        <div class="stats-row">
          <div class="stat-box">
            <div class="stat-n">8<sup>+</sup></div>
            <div class="stat-l">Years</div>
          </div>
          <div class="stat-box">
            <div class="stat-n">4<sup>개</sup></div>
            <div class="stat-l">금융 프로젝트</div>
          </div>
          <div class="stat-box">
            <div class="stat-n">PL</div>
            <div class="stat-l">현재 역할</div>
          </div>
        </div>
        <div class="skill-list">
          <div class="skill-row">
            <div class="skill-dot"></div>
            <div class="skill-txt">
              <strong>서비스 기획 · 화면 설계</strong>
              요구사항 분석, 플로우 정의, 스토리보드 작성
            </div>
          </div>
          <div class="skill-row">
            <div class="skill-dot"></div>
            <div class="skill-txt">
              <strong>UIUX 기획 · 정책 설계</strong>
              인터넷뱅킹, 스마트뱅킹, 모바일 앱 UIUX 전반
            </div>
          </div>
          <div class="skill-row">
            <div class="skill-dot"></div>
            <div class="skill-txt">
              <strong>프로젝트 리딩 (PL)</strong>
              일정 관리, 이해관계자 협의, 배포 관리
            </div>
          </div>
          <div class="skill-row">
            <div class="skill-dot"></div>
            <div class="skill-txt">
              <strong>사용자 리서치</strong>
              현장 방문 인터뷰, 니즈 파악, 개선안 도출
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="port" id="port-1" data-nav="port">
    <div class="sec-head">
      <span class="sec-num">01</span>
      <div class="sec-meta">
        <p class="sec-lbl">금융 SI · 차세대 구축</p>
        <h2 class="sec-title">NH기업뱅킹 차세대 프로젝트</h2>
        <p class="sec-desc">인터넷뱅킹·스마트뱅킹 조회·이체 전반의 UIUX 개편, 금융 허브 연계 시뮬레이션 서비스 기획, 비대면 전자약정 프로세스 설계까지 차세대 구축 전 단계를 경험했습니다.</p>
      </div>
    </div>
    <div class="gallery" id="g1"></div>
  </section>

  <section class="port" id="port-2" data-nav="port">
    <div class="sec-head">
      <span class="sec-num">02</span>
      <div class="sec-meta">
        <p class="sec-lbl">금융 SI · 고도화 운영</p>
        <h2 class="sec-title">NH기업뱅킹 고도화 · 상시운영(SM)</h2>
        <p class="sec-desc">구축 이후 고도화와 상시운영까지 서비스 전 생애주기를 직접 경험했습니다. 현재 2026 도급운영 PL로서 요구사항 협의, 일정 관리, 배포 전 과정을 총괄하고 있습니다.</p>
      </div>
    </div>
    <div class="gallery" id="g2"></div>
  </section>

  <section class="port" id="port-3" data-nav="port">
    <div class="sec-head">
      <span class="sec-num">03</span>
      <div class="sec-meta">
        <p class="sec-lbl">비대면 금융 서비스</p>
        <h2 class="sec-title">비대면 신규가입 · 전자약정 기획</h2>
        <p class="sec-desc">NH기업뱅킹 비대면 법인 입출금·전자금융 가입, 여신 전자약정 프로세스(개인/법인 단독) 등 금융 핵심 로직을 직접 설계했습니다. 복잡한 법적 요건을 사용자 친화적 플로우로 풀어낸 경험입니다.</p>
      </div>
    </div>
    <div class="gallery" id="g3"></div>
  </section>

  <section class="port" id="port-4" data-nav="port">
    <div class="sec-head">
      <span class="sec-num">04</span>
      <div class="sec-meta">
        <p class="sec-lbl">이커머스 · 자사몰 개선</p>
        <h2 class="sec-title">컴퓨존 자사몰 UIUX 기획</h2>
        <p class="sec-desc">약 3년 4개월간 PC·모바일 자사몰 서비스 전반을 기획했습니다. 상품 검색 알고리즘 고도화, 간편결제(토스·카카오) 연계, 마이페이지·장바구니 개선 등 커머스 도메인의 폭넓은 기획 경험을 쌓았습니다.</p>
      </div>
    </div>
    <div class="gallery" id="g4"></div>
  </section>

  <section class="port" id="port-5" data-nav="port">
    <div class="sec-head">
      <span class="sec-num">05</span>
      <div class="sec-meta">
        <p class="sec-lbl">농업 플랫폼 · IoT · 모빌리티</p>
        <h2 class="sec-title">도메인 확장 경험 — 오픈뱅킹·농업·전기차</h2>
        <p class="sec-desc">NH오늘농사 2차 개편(현장 방문 리서치 포함), 하나은행 정보단말 UIUX 정책 기획, 전기차 충전소 앱 기획, 실내 측위 내비게이션 서비스 등 매번 새로운 도메인에 빠르게 적응해 온 경험입니다.</p>
      </div>
    </div>
    <div class="gallery" id="g5"></div>
  </section>

  <section id="contact" data-nav="contact">
    <div class="con-inner">
      <div class="con-badge">Open to new opportunities</div>
      <h2 class="con-title">Let's build<br><em>something that lasts.</em></h2>
      <p class="con-desc">
        Whether it's a new project, a collaboration, or just a hello —<br>I'm always happy to connect.
      </p>
      <div class="con-links">
        <a href="mailto:your-email@example.com" class="con-link">
          <div class="con-icon"><svg viewBox="0 0 24 24"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg></div>
          <span class="con-lbl">Email</span>
        </a>
        <a href="tel:010-7429-3111" class="con-link">
          <div class="con-icon"><svg viewBox="0 0 24 24"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.62 3.38 2 2 0 0 1 3.59 1h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 8.56a16 16 0 0 0 5.94 5.94l1.62-1.62a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/></svg></div>
          <span class="con-lbl">Phone</span>
        </a>
        <a href="#" class="con-link">
          <div class="con-icon"><svg viewBox="0 0 24 24"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-4 0v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg></div>
          <span class="con-lbl">LinkedIn</span>
        </a>
      </div>
      <div class="con-foot">
        <div class="con-line"></div>
        <p class="con-copy">© <span id="footerYear"></span> Kim Deokyong. All rights reserved.</p>
      </div>
    </div>
  </section>

</div><div class="pop-ov" id="popOv" role="dialog" aria-modal="true" aria-labelledby="popTitle">
  <div class="pop-box" id="popBox">
    <button class="pop-close" id="popClose" aria-label="닫기">✕</button>
    <button class="pop-arr prev" id="popPrev" aria-label="이전 작업"><svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"/></svg></button>
    <button class="pop-arr next" id="popNext" aria-label="다음 작업"><svg viewBox="0 0 24 24"><polyline points="9 18 15 12 9 6"/></svg></button>
    <div class="pop-img-wrap">
      <img id="popImg" src="" alt="" loading="lazy">
    </div>
    <div class="pop-info">
      <div class="pop-info-left">
        <div class="pop-title" id="popTitle"></div>
        <div class="pop-sub"   id="popSub"></div>
        <div class="pop-desc"  id="popDesc"></div>
        <div class="pop-tags"  id="popTags"></div>
      </div>
      <div class="pop-info-right">
        <a href="#" class="pop-link" id="popExtLink" target="_blank" rel="noopener">
          상세 보기
          <svg viewBox="0 0 24 24"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </a>
        <span class="pop-counter" id="popCounter"></span>
      </div>
    </div>
  </div>
</div>

<button class="top-btn" id="topBtn" aria-label="맨 위로">
  <svg viewBox="0 0 24 24"><polyline points="18 15 12 9 6 15"/></svg>
</button>

<script>
'use strict';

/* ══════════════════════════════════════
    DATA  — 이력서 기반 실제 프로젝트
══════════════════════════════════════ */
const DATA = [
  /* 01 NH 차세대 */
  {id:'g1', items:[
    { t:'인터넷뱅킹 조회·이체 UIUX 개편', s:'NH기업뱅킹 2024 차세대 · 서비스 기획', k:10, desc:'인터넷뱅킹 조회·이체 메뉴 전반의 UIUX 흐름을 재정의했습니다. 기존 메뉴 구조의 복잡도를 줄이고, 반복 사용자의 효율적인 동선을 중심으로 화면을 설계했습니다.', tags:['UIUX 기획','인터넷뱅킹','플로우 설계'], link:'#' },
    { t:'금융 허브 연계 시뮬레이션 서비스 기획', s:'NH기업뱅킹 2024 차세대 · 서비스 기획', k:20, desc:'금융 허브와 연계한 시뮬레이션 서비스를 기획하고 chart.js 기반 샘플 HTML까지 직접 작성했습니다. 데이터 시각화와 기획의 경계를 직접 연결한 경험이었습니다.', tags:['시뮬레이션 기획','데이터 시각화','chart.js'], link:'#' },
    { t:'입출금알림PUSH · 결재함 서비스 개선', s:'NH기업뱅킹 2024 차세대 · 서비스 기획', k:30, desc:'실시간 알림 서비스의 발송 조건 재설계와 결재함 UI 개선을 담당했습니다. 기업 사용자가 많은 만큼 오류 발생 시 임팩트가 크기 때문에 예외 케이스를 최우선으로 정의했습니다.', tags:['PUSH 기획','결재함 개선','예외처리'], link:'#' },
    { t:'스마트뱅킹 이체내역·거래내역 UIUX 개선', s:'NH기업뱅킹 2024 차세대 · 서비스 기획', k:40, desc:'모바일 환경에서 이체·거래 내역 조회 흐름과 필터 구조를 재설계했습니다. 빈번하게 사용되는 기능인 만큼, 한 화면에서 원하는 정보를 바로 찾을 수 있는 구조에 집중했습니다.', tags:['스마트뱅킹','조회 UX','모바일 기획'], link:'#' },
  ]},
  /* 02 NH 고도화·운영 */
  {id:'g2', items:[
    { t:'2026 도급운영(SM) PL 기획 총괄', s:'NH기업뱅킹 2026 · 상시운영 PL', k:50, desc:'운영 기간 전체의 일정 관리, 요구사항 사전 협의, 배포 관리까지 PL로서 운영 전반을 총괄하고 있습니다.', tags:['PL 업무','일정 관리','제안 발표'], link:'#' },
    { t:'비대면 개인사업자 오픈뱅킹 서비스 기획', s:'NH기업뱅킹 2023 디지털 플랫폼 고도화', k:60, desc:'스마트·인터넷 개인사업자 채널의 오픈뱅킹 파트를 기획했습니다. 타 금융기관 계좌 연결 흐름과 인증 프로세스를 중심으로 사용자 관점의 단계 최소화에 집중했습니다.', tags:['오픈뱅킹','비대면','고도화'], link:'#' },
    { t:'2025 도급운영(SM) 경쟁 입찰 제안 발표', s:'NH기업뱅킹 2025 · 제안 발표', k:70, desc:'제안 자료 제작 30%와 발표 100%를 담당했습니다. 고객사의 요구사항을 정확히 반영한 제안 전략을 수립했습니다.', tags:['제안서','발표','경쟁 입찰'], link:'#' },
    { t:'하나은행 정보단말 공통 UIUX 정책 기획', s:'2025 하나은행 정보단말 고도화 · 단독 기획', k:80, desc:'단독으로 하나은행 정보단말의 공통 UIUX 정책을 처음부터 끝까지 설계했습니다. 일관된 인터랙션 가이드라인을 정의했습니다.', tags:['UIUX 정책','단독 기획','정보단말'], link:'#' },
  ]},
  /* 03 비대면 금융 */
  {id:'g3', items:[
    { t:'비대면 여신 전자약정 프로세스 설계', s:'NH기업뱅킹 2025 고도화 · 핵심 기획', k:90, desc:'개인 및 법인 단독 여신 전자약정 프로세스의 서비스 흐름과 UIUX를 설계했습니다.', tags:['전자약정','여신 기획','법인 서비스'], link:'#' },
    { t:'비대면 법인 전자금융 가입 서비스 기획', s:'NH기업뱅킹 2025 고도화 · 핵심 기획', k:100, desc:'법인 단독 전자금융 및 입출금 계좌 신규 가입 서비스의 플로우와 화면을 설계했습니다.', tags:['비대면 가입','법인 서비스','인증 기획'], link:'#' },
    { t:'비대면 신규가입 · 비대면 전자약정 기획', s:'NH농협은행 프로젝트 · 핵심 성과', k:110, desc:'NH농협은행의 핵심 비대면 금융 서비스 기획을 담당하며 서비스 기획자로서의 기반을 다졌습니다.', tags:['비대면','신규가입','핵심 금융'], link:'#' },
    { t:'금융 서비스 기획 프로세스 체계화', s:'블루비 · 셉템 CX기획팀 · 방법론', k:120, desc:'Figma·Notion·Google Sheets를 활용해 이해관계자와의 실시간 소통 구조를 만들었습니다.', tags:['기획 방법론','Figma','Notion'], link:'#' },
  ]},
  /* 04 커머스 */
  {id:'g4', items:[
    { t:'자사몰 메인·카테고리 광고 배너 UIUX 개선', s:'(주)컴퓨존 자사몰 · Web 기획', k:130, desc:'PC 자사몰의 메인과 카테고리 페이지 내 광고 배너 배치 및 클릭 유도 구조를 개선했습니다.', tags:['광고 배너','Web 기획','CRO'], link:'#' },
    { t:'앱 메인·상품 상세 페이지 개편', s:'(주)컴퓨존 자사몰 · App 기획', k:140, desc:'인기 상품 및 검색량 기반 시각화 서비스를 기획했습니다.', tags:['앱 기획','상품 상세','시각화'], link:'#' },
    { t:'모바일 검색 알고리즘 고도화 기획', s:'(주)컴퓨존 자사몰 · 검색 UX', k:150, desc:'모바일 상품 검색 기능의 정렬 알고리즘 고도화와 인기 검색어 기능 기획을 담당했습니다.', tags:['검색 UX','알고리즘','인기 검색어'], link:'#' },
    { t:'간편결제 연계 서비스 기획 (토스·카카오)', s:'(주)컴퓨존 자사몰 · 결제 기획', k:160, desc:'토스페이와 카카오페이 간편결제 연계 서비스를 기획했습니다.', tags:['간편결제','토스페이','카카오페이'], link:'#' },
  ]},
  /* 05 도메인 확장 */
  {id:'g5', items:[
    { t:'NH오늘농사 2차 개편 — 현장 리서치 기반', s:'블루비 · NH오늘농사 프로젝트', k:170, desc:'지역 농협 및 한국농수산식품유통공사를 직접 방문해 실제 사용자 니즈를 파악했습니다.', tags:['현장 리서치','농업 플랫폼','사용자 인터뷰'], link:'#' },
    { t:'NH오늘농사 APP PUSH 발송 조건 개선', s:'블루비 · NH오늘농사 프로젝트', k:180, desc:'PUSH 발송 조건 로직을 재설계했습니다.', tags:['PUSH 기획','배치 설계','농업 플랫폼'], link:'#' },
    { t:'전기차 충전소 앱 신규 서비스 기획', s:'(주)엠투브 · 한국전력 고객사', k:190, desc:'한국전력을 고객사로 전기차 충전소 앱 신규 서비스와 UIUX를 처음부터 기획했습니다.', tags:['전기차','앱 기획','모빌리티'], link:'#' },
    { t:'실내 측위 내비게이션 서비스 고도화', s:'(주)엠투브 · 자사 솔루션', k:200, desc:'실내 측위 기술 기반의 내비게이션 서비스 고도화와 기술 테스트를 지원했습니다.', tags:['IoT','실내 측위','기술 기획'], link:'#' },
  ]},
];

const FLAT = DATA.flatMap(d => d.items);

/* ══════════════════════════════════════
    1. 갤러리 빌드 (인터랙션 바인딩 포함)
══════════════════════════════════════ */
DATA.forEach(d => {
  const grid = document.getElementById(d.id);
  if (!grid) return;
  d.items.forEach(item => {
    const idx = FLAT.indexOf(item);
    const el  = document.createElement('div');
    el.className = 'g-item';
    el.setAttribute('role','button');
    el.setAttribute('tabindex','0');
    el.setAttribute('aria-label', item.t + ' 상세 보기');
    el.innerHTML =
      `<img src="https://picsum.photos/seed/${item.k}/600/450" alt="${item.t}" loading="lazy">` +
      `<div class="g-over"><span>자세히 보기</span></div>`;
    
    // 클릭 이벤트
    el.addEventListener('click', () => openPopup(idx));
    el.addEventListener('keydown', e => {
      if (e.key==='Enter'||e.key===' '){ e.preventDefault(); openPopup(idx); }
    });

    // 마우스 커서 반응
    el.addEventListener('mouseenter', () => { if(typeof cr !== 'undefined') cr.classList.add('big'); });
    el.addEventListener('mouseleave', () => { if(typeof cr !== 'undefined') cr.classList.remove('big'); });

    // 3D Tilt 효과
    el.addEventListener('mousemove', e => {
      const r = el.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width - 0.5;
      const py = (e.clientY - r.top) / r.height - 0.5;
      el.style.transform = `perspective(600px) rotateY(${px * 8}deg) rotateX(${-py * 5}deg) scale(1.02)`;
    });
    el.addEventListener('mouseleave', () => { el.style.transform = ''; });

    grid.appendChild(el);
  });
});

/* ══════════════════════════════════════
    2. 공통 UI 로직
══════════════════════════════════════ */
document.getElementById('footerYear').textContent = new Date().getFullYear();

const wrap     = document.getElementById('wrap');
const sects    = Array.from(wrap.querySelectorAll('section'));
const navAs    = document.querySelectorAll('.nl');
const dotsDiv  = document.getElementById('dots');
const topBtn   = document.getElementById('topBtn');
const nav      = document.querySelector('nav');
const DARK_SECTS = new Set(['about', 'contact']);

// 도트 네비게이션 생성
const DOT_MAP = { main:'홈', about:'소개', contact:'컨택트' };
sects.forEach((s, i) => {
  const navKey = s.dataset.nav;
  const portSects = sects.filter(ss=>ss.dataset.nav==='port');
  const lbl = navKey==='port' ? `0${portSects.indexOf(s)+1}` : (DOT_MAP[navKey]||'');
  const d = document.createElement('div');
  d.className = 'dot' + (i===0 ? ' on' : '');
  d.setAttribute('data-label', lbl);
  d.setAttribute('role','button');
  d.setAttribute('tabindex','0');
  d.addEventListener('click', () => wrap.scrollTo({top:s.offsetTop, behavior:'smooth'}));
  dotsDiv.appendChild(d);
});
const dotEls = Array.from(dotsDiv.querySelectorAll('.dot'));

// 스크롤 감지 (IntersectionObserver)
const io = new IntersectionObserver(entries => {
  entries.forEach(entry => {
    if (!entry.isIntersecting) return;
    const idx = sects.indexOf(entry.target);
    const key = entry.target.dataset.nav;
    navAs.forEach(a => a.classList.toggle('active', a.dataset.nav===key));
    dotEls.forEach((d,i) => d.classList.toggle('on', i===idx));
    topBtn.classList.toggle('vis', idx > 0);
    nav.classList.toggle('on-dark', DARK_SECTS.has(key));
  });
}, {root: wrap, threshold: 0.55});
sects.forEach(s => io.observe(s));

// 네비게이션 클릭 이벤트
const ham = document.getElementById('ham');
const navLinksEl = document.getElementById('navLinks');

document.querySelectorAll('a[href^="#"]').forEach(a => {
  a.addEventListener('click', e => {
    const t = document.querySelector(a.getAttribute('href'));
    if (!t) return;
    e.preventDefault();
    wrap.scrollTo({top:t.offsetTop, behavior:'smooth'});
    navLinksEl.classList.remove('open');
    ham.classList.remove('open');
  });
});

topBtn.addEventListener('click', () => wrap.scrollTo({top:0, behavior:'smooth'}));
ham.addEventListener('click', () => {
  const o = navLinksEl.classList.toggle('open');
  ham.classList.toggle('open', o);
});

/* ══════════════════════════════════════
    3. 메인 인터랙션 (커서, 리플, 패럴랙스)
══════════════════════════════════════ */
const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
const cd = document.getElementById('c-dot');
const cr = document.getElementById('c-ring');
const gb = document.getElementById('gridBg');
const gl = document.getElementById('glowEl');
const cv = document.getElementById('ripple');
const ctx = cv.getContext('2d');
const popOvEl = document.getElementById('popOv');

let curTx=0, curTy=0, curRx=0, curRy=0;
let parPx=0, parPy=0, parCx=0, parCy=0;
let ripTx=0, ripTy=0, ripCx=0, ripCy=0;
let W, H;
const rings = [];
let lastSpawn = 0;

const resizeCv = () => { W=cv.width=innerWidth; H=cv.height=innerHeight; };
resizeCv();
window.addEventListener('resize', resizeCv, {passive:true});

function spawnRings(x, y, now) {
  if(now - lastSpawn < 300) return; 
  lastSpawn = now;
  for(let i=0; i<3; i++) rings.push({x, y, r:i*12, max:60+i*28, a:0.13-i*0.035, born:now+i*5.5, life:1300+i*180});
}

if (!prefersReduced) {
  document.addEventListener('mousemove', e => {
    curTx = e.clientX; curTy = e.clientY;
    cd.style.left = curTx + 'px'; cd.style.top = curTy + 'px';
    cd.style.opacity = '1'; cr.style.opacity = '1';
    parPx = (e.clientX / innerWidth - 0.5) * 2;
    parPy = (e.clientY / innerHeight - 0.5) * 2;
    if(!popOvEl.classList.contains('open')){ ripTx = e.clientX; ripTy = e.clientY; }
  }, {passive:true});

  document.addEventListener('mouseleave', () => { cd.style.opacity='0'; cr.style.opacity='0'; });
  
  // 정적 엘리먼트 호버 바인딩
  document.querySelectorAll('a, button, [role="button"]').forEach(el => {
    el.addEventListener('mouseenter', () => cr.classList.add('big'));
    el.addEventListener('mouseleave', () => cr.classList.remove('big'));
  });

  (function mainLoop(ts){
    requestAnimationFrame(mainLoop);
    // 커서 스무딩
    curRx += (curTx - curRx) * 0.15; curRy += (curTy - curRy) * 0.15;
    cr.style.left = curRx + 'px'; cr.style.top = curRy + 'px';
    // 배경 패럴랙스
    parCx += (parPx - parCx) * 0.05; parCy += (parPy - parCy) * 0.05;
    const ptx = parCx * 12, pty = parCy * 10;
    gb.style.transform = `translate(${ptx}px, ${pty}px)`;
    gl.style.transform = `translate(calc(-50% + ${ptx*1.6}px), calc(-50% + ${pty*1.6}px))`;
    // 리플 캔버스
    ctx.clearRect(0, 0, W, H);
    ripCx += (ripTx - ripCx) * 0.07; ripCy += (ripTy - ripCy) * 0.07;
    spawnRings(ripCx, ripCy, ts);
    for(let i=rings.length-1; i>=0; i--){
      const rg = rings[i];
      if(ts < rg.born) continue;
      const p = Math.min((ts - rg.born) / rg.life, 1);
      if(p >= 1) { rings.splice(i, 1); continue; }
      const r = rg.r + (rg.max - rg.r) * (1 - (1 - p)**3);
      const a = rg.a * (1 - p)**2;
      ctx.beginPath();
      ctx.arc(rg.x, rg.y, r, 0, Math.PI * 2);
      ctx.strokeStyle = `rgba(45, 91, 227, ${a})`;
      ctx.lineWidth = 0.8; ctx.stroke();
    }
  })(0);
}

/* ══════════════════════════════════════
    4. POPUP 로직
══════════════════════════════════════ */
const popOv      = document.getElementById('popOv');
const popBox     = document.getElementById('popBox');
const popImg     = document.getElementById('popImg');
const popTitle   = document.getElementById('popTitle');
const popSub     = document.getElementById('popSub');
const popDesc    = document.getElementById('popDesc');
const popTags    = document.getElementById('popTags');
const popCounter = document.getElementById('popCounter');
const popExtLink = document.getElementById('popExtLink');
let pidx=0, scrollLockY=0;

function openPopup(i){
  pidx = i; renderPop();
  popOv.classList.add('open');
  scrollLockY = wrap.scrollTop;
  wrap.style.overflow = 'hidden';
}
function closePop(){
  popOv.classList.remove('open');
  wrap.style.overflow = '';
  wrap.scrollTop = scrollLockY;
}
function renderPop(){
  const item = FLAT[pidx];
  popImg.style.opacity = '0';
  const im = new Image();
  im.src = `https://picsum.photos/seed/${item.k}/1200/800`;
  im.onload = () => { popImg.src = im.src; popImg.style.opacity = '1'; };
  popTitle.textContent = item.t;
  popSub.textContent = item.s;
  popDesc.textContent = item.desc || '';
  popTags.innerHTML = '';
  (item.tags || []).forEach(tag => {
    const sp = document.createElement('span');
    sp.className = 'pop-tag'; sp.textContent = tag;
    popTags.appendChild(sp);
  });
  popCounter.textContent = `${pidx + 1} / ${FLAT.length}`;
  popExtLink.href = item.link || '#';
}

document.getElementById('popClose').addEventListener('click', closePop);
popOv.addEventListener('click', e => { if(e.target === popOv) closePop(); });
document.getElementById('popPrev').addEventListener('click', () => { pidx = (pidx - 1 + FLAT.length) % FLAT.length; renderPop(); });
document.getElementById('popNext').addEventListener('click', () => { pidx = (pidx + 1) % FLAT.length; renderPop(); });

document.addEventListener('keydown', e => {
  if(!popOv.classList.contains('open')) return;
  if(e.key === 'Escape') closePop();
  if(e.key === 'ArrowLeft') { pidx = (pidx - 1 + FLAT.length) % FLAT.length; renderPop(); }
  if(e.key === 'ArrowRight') { pidx = (pidx + 1) % FLAT.length; renderPop(); }
});

let touchSX = 0;
popOv.addEventListener('touchstart', e => { touchSX = e.touches[0].clientX; }, {passive:true});
popOv.addEventListener('touchend', e => {
  const dx = e.changedTouches[0].clientX - touchSX;
  if(Math.abs(dx) < 50) return;
  if(dx > 0) pidx = (pidx - 1 + FLAT.length) % FLAT.length;
  else pidx = (pidx + 1) % FLAT.length;
  renderPop();
});
</script>
</body>
</html>
